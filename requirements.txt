# Google Workspace MCP Server Requirements

# MCP Framework
# Upper bound added 2026-08-12: mcp 2.0.0 removed `Server.list_tools`, so CI
# (which resolves the newest release) failed collecting tests/test_server.py
# with AttributeError, while local envs pinned at 1.27.0 passed. Lift this only
# together with a real 2.0 migration.
mcp>=1.22.0,<2

# Security: Pin starlette to patched version (CVE-2025-62727)
# https://github.com/advisories/GHSA-7f5h-v6xp-fcq8
starlette>=0.49.1

# Google API Client Libraries
google-api-python-client>=2.88.0
google-auth>=2.16.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0

# Email handling
email-validator>=2.0.0

# Utility Libraries
python-dotenv>=1.0.0
aiofiles>=23.1.0
workalendar>=17.0.0

# Testing
pytest>=7.3.0
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0
pytest-cov>=4.1.0

# Development
black>=23.3.0
ruff>=0.1.0
mypy>=1.3.0
types-aiofiles>=23.1.0
bandit>=1.7.0
